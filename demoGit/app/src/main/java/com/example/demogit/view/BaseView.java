package com.example.demogit.view;

import com.example.demogit.bean.User;
import com.example.demogit.bean.User2;

import java.util.List;

//定义了一个IView接口类，
//此类中抽象出所有View共同的方法，
//还有个作用就是为所有的View定义统一的接口
//方便之后在BasePresenter中进行泛型

public interface BaseView {
    //定义统一的空接口
}
